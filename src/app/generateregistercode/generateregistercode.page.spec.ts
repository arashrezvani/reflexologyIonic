import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GenerateregistercodePage } from './generateregistercode.page';

describe('GenerateregistercodePage', () => {
  let component: GenerateregistercodePage;
  let fixture: ComponentFixture<GenerateregistercodePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GenerateregistercodePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
